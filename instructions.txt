#Setup

#open a powershell terminal

py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
python -m pytest



#make sure openclaw gateway is accepting commands


#run
cd G:\images\source_code\kadathic_core
pip install -e ".[dev]"

#quick test

agentfoundry --help
agentfoundry --config examples/sample_project/agentfoundry.yaml agents list
agentfoundry --config examples/sample_project/agentfoundry.yaml agents show chat_companion
